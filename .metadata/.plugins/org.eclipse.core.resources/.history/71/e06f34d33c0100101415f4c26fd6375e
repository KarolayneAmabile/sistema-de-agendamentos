package models;

// é um metodo que estará presente nas camadas do MVC e será chamado sempre
// que houver alguma mudança
public interface SalonListener {
	// notifica sobre agendamento e preço
	public void updateAppointment(SalonEvent event);
	// notifica novo serviço
	public void updateService(SalonEvent event);

}
