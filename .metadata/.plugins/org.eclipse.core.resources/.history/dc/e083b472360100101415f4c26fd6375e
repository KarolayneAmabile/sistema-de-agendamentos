package factory;

import java.math.BigDecimal;

import exceptions.StrException;
import models.Service;

public class SalonFactory {
	public void createService(int id, String serviceName, BigDecimal price) {
		try {
			Service service = new Service(id, serviceName, price);
		} catch (StrException e) {
			
		}
	}
}
