package teste;

import java.math.BigDecimal;
import java.util.Date;

import models.Appointment;
import models.Salon;
import models.Service;

public class oi {
	public static void main(String[] str) {
		Salon salon = new Salon();
		
		salon.addService(1, "Chapinha", new BigDecimal(50.0));
		
		salon.addAppointment(new Appointment(1, "Karol", new Date(), new Service());
		salon.addAppointment(new Appointment(1, "Karol", new Date(), new Service(1, "Chapinha", new BigDecimal(50.0)));
		
	}
}
